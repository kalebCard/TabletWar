import Phaser from 'phaser';

export class InteractionManager {
    private scene: Phaser.Scene;

    constructor(scene: Phaser.Scene) {
        this.scene = scene;
    }

    public setupInteraction() {
        // Drag, drop, measure logic to be migrated
    }
}
